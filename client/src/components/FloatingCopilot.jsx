import React from 'react';
import AIFloatingButton from './ai/AIFloatingButton';

const FloatingCopilot = ({ jobId }) => {
  return <AIFloatingButton jobId={jobId} />;
};

export default FloatingCopilot;
